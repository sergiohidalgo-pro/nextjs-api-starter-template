# Authentication Configuration
# IMPORTANT: Change these values in production\!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-at-least-32-characters
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Default admin credentials (change in production)
AUTH_USERNAME=admin
AUTH_PASSWORD=secure-password-123
AUTH_2FA_SECRET=JBSWY3DPEHPK3PXP

# Application Configuration  
NODE_ENV=development
API_BASE_URL=http://localhost:3000
PORT=3000

# Rate Limiting
RATE_LIMIT_MAX_REQUESTS=5

# Database Configuration
# Choose ONE of the following MongoDB connection strings:

# Option 1: Local MongoDB (default for development)
MONGODB_URI=mongodb://localhost:27017/nextjs-api

# Option 2: Docker MongoDB (when using docker-compose)
# MONGODB_URI=mongodb://mongo:27017/nextjs-api

# Option 3: Production MongoDB Atlas
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# MongoDB Root Credentials (for Docker containers)
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=password

# Security Headers (optional)
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Logging Configuration (optional)
# LOG_LEVEL=info
# LOG_FORMAT=json

# Feature Flags (optional)
# ENABLE_SWAGGER_UI=true
# ENABLE_HEALTH_CHECKS=true
# ENABLE_RATE_LIMITING=true
EOF < /dev/null